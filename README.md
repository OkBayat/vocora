# واژه‌یار IELTS

یک جعبه لایتنر آفلاین برای تمرین املای لغات IELTS Listening. هیچ دیتابیسی ندارد و همه‌ی اطلاعات را در `localStorage` مرورگر ذخیره می‌کند.

مخزن پروژه: [OkBayat/el2-leitner](https://github.com/OkBayat/el2-leitner)

## قانون مرور

- ابتدای هر روز تقویمی ۱۰ لغت جدید وارد خانهٔ ۱ می‌شود.
- انتقال خانهٔ ۲ به ۳ پس از ۲ روز، خانهٔ ۳ به ۴ پس از ۳ روز و خانهٔ ۴ به ۵ پس از ۷ روز انجام می‌شود.
- پاسخ اشتباه کارت را به خانهٔ ۱ بازمی‌گرداند و ارتقای آن را تا روز بعد قفل می‌کند.
- تمرین آزاد خانهٔ ۱ نامحدود است، اما جای کارت‌ها را تغییر نمی‌دهد.
- لغات دارای خطای بیشتر در تمرین آزاد با احتمال بیشتری نمایش داده می‌شوند.
- از بانک واژه‌ها می‌توان هر لغت واردنشده را با دکمهٔ `+` به خانهٔ ۱ فرستاد.
- از صفحهٔ Home می‌توان تعداد دلخواهی لغت جدید انتخاب و آزمون اولیهٔ آن‌ها را بلافاصله شروع کرد؛ پاسخ درست مستقیم به خانهٔ ۲ می‌رود و پاسخ غلط در خانهٔ ۱ می‌ماند.

## اجرا

فایل `index.html` را با Chrome، Edge یا Firefox باز کنید. فهرست ۱۵۰۰ لغت از قبل داخل برنامه قرار دارد.

برای اطمینان بیشتر از ذخیره‌شدن داده‌ها یا استفاده روی موبایل، می‌توانید پوشه را با یک وب‌سرور ساده باز کنید:

```bash
python -m http.server 8080
```

سپس آدرس `http://localhost:8080/leitner-ielts/` را باز کنید.

## پشتیبان‌گیری

- «پشتیبان کامل» تمام لغات، تنظیمات و تاریخچه را نگه می‌دارد.
- «خروجی برای تحلیل با ChatGPT» یک JSON خلاصه و امن از وضعیت یادگیری می‌سازد.
- حذف داده‌های مرورگر، استفاده از حالت ناشناس یا تغییر مرورگر می‌تواند داده‌ها را پاک کند؛ به‌طور منظم پشتیبان بگیرید.

## ورود فایل جدید

فایل Markdown یا TXT باید هر واژه را در یک خط شماره‌گذاری‌شده داشته باشد. تیترهای `##` به عنوان دسته‌بندی ذخیره می‌شوند:

```md
## Travel
1. accommodation
2. itinerary
3. centre / center
```

موارد جداشده با `/` به‌عنوان املای جایگزین پذیرفته می‌شوند.

هنگام ورود، تمام املاهای اصلی و جایگزین با بانک موجود و با سایر موارد همان فایل مقایسه می‌شوند. مقایسه به حروف بزرگ و کوچک یا فاصله‌های اضافی حساس نیست و موارد تکراری بدون تغییر بانک رد می‌شوند.
